// Значення, які підставляє esbuild на збірці (build.mjs, опція define).
//
// Це не змінна й не константа в звичайному сенсі: у зібраному бандлі на її
// місці стоїть літерал. TypeScript про такі підстановки не знає нічого, тому
// тип оголошується тут — інакше кожне використання було б помилкою компіляції.

// Базова адреса нашого бекенду. Dev-збірка підставляє http://localhost:8080,
// прод-збірка — реальний https-домен (і падає, якщо домен локальний або http).
//
// Свідомо НЕ береться зі storage: адреса бекенду не є налаштуванням
// користувача. Можливість змінити її в рантаймі означала б, що підміна адреси
// віддає токени акаунта сторонньому серверу.
declare const __BACKEND_BASE__: string;
